# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.backup
.env.*.backup

# Firebase service account credentials (never commit)
serviceAccountKey.json

# Firestore migration backups (contain customer data, never commit)
sales-backup-*.json
backups/

# Temporary and backup files
*.tmp
*.backup
*.clean

# Local-only Claude Code settings (machine-specific tool permissions, absolute local paths)
.claude/settings.local.json

# Generated invoice PDFs (test/manual-QA artifacts produced at the repo root, never commit)
/Invoice_*.pdf
